function response(room, msg, sender, isGroupChat, replier, imageDB, packageName) {
  if (sender == "ᴳᵉᵒⁿʷᵒᵒ ᴷⁱᵐ" && msg.startsWith(".eval "))
  try {
  replier.reply(eval(msg.substr(6)));
  } catch (e) {
    replier.reply(e);
  }
}
